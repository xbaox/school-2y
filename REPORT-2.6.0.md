# Релиз 2.6.0 «Пересборка урока»

Тег отката: `v2.5.0`. Откатиться целиком — `git reset --hard v2.5.0`, поднять
`VERSION` в `sw.js` и задеплоить.

Тесты: **783 проверки, все зелёные** (`node tests/run.js`). Было 493 при трёх
падающих.

---

## Зачем этот релиз

Урок занимал ~2 часа вместо заявленных 40 минут. Задания приходили пачками по
6–8. Из 24 долгов закрылся один. Фидбек приходил по-английски и был непонятен.
Выученные слова копились в колоде без пенсии. 26 августа урок был брошен на
середине — это и стало поводом.

---

## Что сделано по этапам

### Этап 1 · Экспресс-диагностика

Четыре гипотезы проверены по коду, каждая перепроверена независимо.

| Гипотеза | Вердикт |
|---|---|
| «Погашено» — точное строковое сравнение | **не подтвердилась** |
| «В разогрев» рвёт строки с числами и точками | подтвердилась |
| Минимумы и длительность живут в `prompts.js` | частично: они в `steps.js` |
| A-02 падает из-за дат, прибитых к 2026-08-20 | подтвердилась |

Главная находка: `matchDebt` никогда не сравнивал строки точно — он нормализовал
текст и матчил нечётко. Но пороги (`ratio ≥ 0.5`, пословно `≥ 0.6`) были так
узки, что из восьми реальных вариантов «Погашено» проходил только дословный.
Болезнь оказалась не в отсутствии нормализации, а в порогах.

Вторая находка: тест A-02 краснел не из-за правила «два урока подряд», а из-за
более раннего правила «серия сломалась». Он протух сам собой 23 августа.

### Этап 2 · Промпт урока v2

Контракт преподавателя переписан целиком: 12 правил → 16, вставлен дословно.

* **Правило 1** — одно задание за сообщение, пачки запрещены.
* **Правило 2** — ритм спринтами, общее число заданий на урок.
* **Правило 3** — стоп-слово «стоп»/«устал» сворачивает урок без вины.
* **Правило 4** — фото-канал: математика решается на бумаге, проверяется как
  экзаменатор Онтарио.
* **Правило 5** — русский дубль обязателен всегда, даже когда подача 0% русского.
* **Правило 14** — долги по id; работает и до введения id, и после.

Промпт самодостаточен: ссылок на внешние «правила проекта» нигде нет.

Ритм спринтов считается от длительности: спринтов 2, после 40 минут — 3, длина
`round(lessonMin / спринтов)`. Разгон Ф0 даёт «2 спринта × ~15–18′».

Поштучные минимумы этапов (разогрев 6–8, разбор 4–6, практика ≥10) сняты — в
сумме они давали 20–24 задания и спорили бы с правилом 2. Вместо них мягкая
разбивка: «разогрев 3–4 задания · практика 6–8 · письмо 1–2 — в сумме 10–12».

**Beta-деплой сделан сразу после этого этапа** — `v2.6.0-beta`, чтобы промпт был
у студента к уроку 27.08, не дожидаясь остального.

### Этап 3 · Долги 2.0

У долга появился короткий стабильный id: `D-1`, `D-2`… Миграция раздала id всем
существующим долгам по порядку создания, идемпотентно, номера не переиспользует.
В промпт долги уходят как `[D-7] текст`.

`matchDebt` — два яруса:

1. **id** вида `D-7` из строки. Якорь, который ИИ не перефразирует.
2. **текст**: формулировка долга целиком внутри строки (дословная копия с
   дописанным «— отработано») либо похожесть ≥0.85 по коэффициенту Дайса.

Старые ярусы 0.5/0.6 удалены. Обратное направление подстроки («знак наклона»
внутри «путает знак наклона при отрицательном k») теперь судит только похожесть,
поэтому короткий огрызок чужой долг не гасит.

Несопоставленные строки «Погашено» больше не глотаются молча: подтверждение
парсера показывает «Не сопоставлено: N строк — проверь id».

Прогресс «погашено 1/2» виден в Журнале и на карточке долга.

### Этап 4 · Шкала нагрузки v2

Механика цикла не тронута. Заменены параметры:

| Позиция | Урок | Заданий | Старт | Перенос |
|---|---|---|---|---|
| S1 | 35′ | 12–14 | L1 | 1 |
| S2 | 40′ | 14–16 | L1 | 1–2 |
| S3 | 45′ | 16–18 | L2 | 2 |
| S4 | 50′ | 18–20 | L2 | 2–3 |
| Г1–Г3 | 50′ | 18–20 | L2 (Г3 — финиш L3) | 3 |

Колонка «минимум вопросов» заменена диапазоном заданий. Г2: «русский → 0»
переписан на «подача без русского (перевод фидбека остаётся!)».

**Разгон Ф0**: до 2026-09-07 включительно урок держится на 30–35′ и 10–12
заданиях независимо от позиции шкалы. Граница совпадает со стартом школы 08.09 —
разгон выключается сам.

### Этап 5 · Сжатие Ф0 и каркас Ф1

Девять уроков помечены пропущенными: B3.2, B3.4, B4.2, B4.4, B5.1, B5.2, B5.4,
B6.3, B6.4. Остаются десять: B1.3, B1.4, B2.4, B3.1, B3.3, B4.1, B4.3, B5.3,
B6.1, B6.2.

Пропущенный урок не входит в прогресс блока, не заводится в состоянии, водопад
его не назначает, блок закрывается по живым урокам. В Программе он виден, но
зачёркнут и подписан «пропущен: сжатие Ф0».

Новые дедлайны: Б2 27.08 · Б1 29.08 · Б3 01.09 · Б4 03.09 · Б5 04.09 · Б6 07.09.
Миграция схемы 2 проставляет их один раз; после неё ручная правка не затирается.

`PACE.RATE_SUMMER` 1.3 → 1: светофор отражает реальный план — 1 урок в день.
Школьный норматив не тронут.

Б6.1 и Б6.2 получили по задаче CEMC-стиля ⭐ — опциональной, в конце фокуса.

Блоки Б7–Б16 переименованы под школьную лестницу. Контент уроков придёт
отдельным пакетом.

Движок понимает `type: 'contest'` — упрощённый шаблон для субботних олимпиадных
уроков: три задачи по одной, полное авторское решение после каждой, без разогрева,
видео и письма. Формат «ИТОГА УРОКА» тот же.

### Этап 6 · Карточки SRS-lite

Три статуса слова: `new` → `learning` → `known`. Три верных подряд делают слово
выученным, ошибка на выученном возвращает в `learning` с обнулённой серией.
Выученное всплывает через 4, затем 10, затем 21 день и после третьего повтора
засыпает совсем.

Карточная сессия получила оценки: тап переворачивает, дальше «знал / не знал».
Раньше оценок сознательно не было; без них SRS невозможен, а время минималки не
растёт — колода, наоборот, сокращается. Долги оценок не имеют.

Счётчик «активных X · выучено Y» — в сессии, в Журнале и в подписи минималки.

«Слова урока»: по любому закрытому уроку открывается список слов со статусом,
серией и датой повтора. Вход — из ленты итогов в Журнале и с карточки закрытого
урока на «Сегодня».

Выученные слова не попадают в блок ПАМЯТЬ промпта.

### Этап 7 · Мелкие фиксы

**7.1** Тест A-02 привязан к `State.today()`.

**7.2** Из `toList` убран `.replace(/\s(\d+)[).]\s/g, '\n')`. Он должен был
срезать нумерацию «1) … 2) …», но делал границей элемента любые «пробел + цифры +
точка-или-скобка + пробел», и само число исчезало. Ломались все четыре списочных
поля. Маркер списка теперь требует пробела после себя — «-3 < x < 5» не теряет
минус.

**7.3** Поток «Вчерашний урок не закрыт» проверен на кейсе B2.4. «Урок не
состоялся» возвращает урок в очередь без штрафа.

---

## Как проверить руками за 5 минут

1. **Промпт.** «Сегодня» → раскрыть пункт урока → «Скопировать промпт» → вставить
   в блокнот. Найти `[КОНТРАКТ ПРЕПОДАВАТЕЛЯ — 16 правил]`. Проверить правило 1
   («ОДНО задание за сообщение»), правило 3 (стоп-слово), правило 4 (ФОТО),
   правило 5 («перевод фидбека обязателен всегда»). Поиском убедиться, что слов
   «правила проекта» в тексте нет.
2. **Карточка урока.** Там же, строка параметров: **«~30–35′ · 10–12 заданий · по
   одному · фото-режим»**. Это разгон Ф0 — до 7 сентября цифры такие независимо от
   ступени.
3. **Долги.** «Журнал» → «Банк долгов». У каждого долга слева `D-1`, `D-2`…, ниже
   «погашено 0/2» или «1/2». В промпте (шаг 1) долги стоят как `[D-2] текст`.
   На следующем уроке попросить ИИ писать в «Погашено» именно id.
4. **Сжатие Ф0.** «Программа» → раскрыть Б5. Три урока зачёркнуты и подписаны
   «пропущен: сжатие Ф0», активен только «3. Речь в классе». Дедлайны блоков:
   Б2 27 авг · Б1 29 авг · Б3 1 сен · Б4 3 сен · Б5 4 сен · Б6 7 сен, все
   светофоры зелёные.
5. **Слова урока.** «Журнал» → тап по любому итогу → кнопка «Слова урока». Список
   со статусами: «новое», «в работе», «выучено», у выученного — дата повтора.
6. **Карточки.** «Журнал» → «Листать». Тап по карточке переворачивает её, снизу
   появляются «не знал» и «знал». Три «знал» подряд по одному слову — тост
   «выучено, вернётся через 4 дня», счётчик «выучено» растёт, слово уходит из
   колоды.
7. **Версия.** «Настройки» → внизу `2.6.0`.

---

## Что отложено

* **Контент Фазы 1.** Блоки Б7–Б16 только переименованы. Детальные уроки придут
  пакетом после школьного расписания (~09–11.09). Тогда же приедут субботние
  уроки с `type: 'contest'` — движок их уже понимает, контента нет.
* **Ручное снятие пометки «пропущен».** Флаг живёт в контенте, а не в состоянии.
  Вернуть урок в программу можно только правкой `content/phase0.js`. Это
  сознательно: контент в БД не хранится.
* **Поле `cleared` в сохранённом итоге.** Список погашенного по-прежнему не
  персистится — живёт только эффект в `debt.clearedIn`. Пересобрать состояние
  долгов из журнала итогов нельзя. ТЗ этого не требовало.
* **Одна арифметическая мелочь.** Ожидалось, что разгон Ф0 даст «2 спринта ×
  ~15–17′»; по формуле `round(35 / 2)` выходит 18, поэтому в промпте стоит
  «~15–18′». Формула реализована буквально.
* **Точка с запятой осталась разделителем** во всех четырёх списочных полях, хотя
  ТЗ 7.2 говорило «сплит только по переводам строк». Причина: так эти поля и
  пишет ИИ («слово — перевод; слово — перевод», «что такое slope?; как найти
  y-intercept?»), а формулу точка с запятой не рвёт — названный дефект она не
  вызывает. Убрана только нумерация, которая и ломала математику.
