# REPORT 2.7.0 «Корень»

Пакет механики и контента Фазы 1 по ТЗ `TZ-v2.7.0-koren.md`.
Восемь этапов, десять коммитов, `2.6.5 → 2.7.0`.

**Корень, который закрыт этим пакетом:** ИИ-преподаватель сочинял то, что должно
быть зафиксировано — определения, критерии, задания, идентичность и закрытие
долгов, — а приложение верило его словам. После пакета контент и приложение
стали источником истины, а ИИ — исполнителем.

---

## Что сделано по этапам

### Этап 1 — таксономия долгов, ремонт банка, дедлайны, серия

- Закрытый список `DEBT_CATS`: 19 категорий (10 письмо, 9 математика). Долг без
  валидного кода завести нельзя; в одной категории не бывает двух открытых.
- Миграция схемы `2 → 3`: слияние дублей по таблице ТЗ, чек-лист языка,
  удаление ложного долга, ремонт банка слов. Новые поля долга: `cat`,
  `examples`, `failedIn`, `lastInjected`, `shownCount`.
- `deadlineSource` (`content | user | spread | shift`): контент снова доезжает
  до пользователя, поставленное руками неприкосновенно.
- Серия: день держат закрытый урок и выполненная минималка; добавки дают очки,
  но серию не держат. Исключение — воскресный радар. `doctrine.js` не изменён.

### Этап 1b — календарь из тестов, `spread`, примеры у долгов

- Набор перестал зависеть от календаря: три кейса краснели не от кода, а от
  того, что прошло время. Раннер получил `withToday(iso, fn)`.
- Четвёртое значение `deadlineSource: 'spread'` — для блоков Ф2–Ф4, которых не
  знает ни один пакет контента. Свежая установка и мигрированное состояние
  совпадают блок в блок.
- Первым примером выжившего долга миграция кладёт живую запись ученика.

### Этап 2 — ИТОГ v3: долг только из таксономии

- Строка «Долги» обязана начинаться с кода категории; без кода отбрасывается
  с уведомлением. Категория с открытым долгом даёт **повтор**: прогресс
  сбрасывается, урок уходит в `failedIn`, пример — в `examples`.
- «Засчитано» (алиас «Погашено» ещё на два релиза): засчитывается только
  показанный в промпте открытый долг, не отмеченный повтором в этом же ИТОГе.
  Закрывает долг по-прежнему лишь второй **разный** урок.
- Чек-лист языка: `state.checklist.stats[i] = {clean, total}`; повторная
  вставка итога статистику не удваивает.
- Разминка пишет в приложение: `РАЗМИНКА: D-1 ✓ D-10 ✗ · слова 15/15`.
  `✓` — только первое касание, `✗` — провал без сброса прогресса.

### Этап 3 — контент Фазы 1

- `CONTENT.registerGlossary` и `CONTENT.glossary`; `content/glossary.js`
  подключён до пакетов фаз. Поле блока `label` → блок B53 показывается как «К».
- Пакет Архитектора подключён целиком: Б7 и Б8 укомплектованы, Б9–Б16 —
  структура, блок К — десять субботних конкурсных уроков.
- Б12 сменил дорожку `biz → write` из контента, без миграции.

### Этап 4 и 4b — промпт v3

- Контракт преподавателя v3 (21 правило) заменил v2 целиком.
- Одиннадцать блоков в порядке ТЗ: контракт → контекст → глоссарий → текст →
  опорные задания → доска долгов → чек-лист языка → разогрев → этапы → итог →
  **ключи в самом конце**.
- Доска долгов заменила окно из пяти FIFO: строка на каждую категорию дорожки,
  до трёх пометок ПРИОРИТЕТ (сначала «1/2», затем давно не показывавшиеся).
- `[ЭТАПЫ УРОКА]` собираются из раскладки ступени, а не сжимаются из
  50-минутного урока.
- Ступень **S0 «Старт школы»**: 30 минут, 8 заданий. Автоперехода нет —
  наверх двигает только кнопка «Стало легко → S1» на «Сегодня».

### Этап 5 — правило «Суббота ⭐»

Новое правило стоит первым в водопаде: у блока К дедлайна нет вовсе, и любое
правило ниже забрало бы субботу себе. Конкурсных уроков не осталось — правило
молчит, и суббота идёт по шаблону недели.

### Этап 6 — колода дня

Группы вместо свалки: подошедшие повторы → слова в работе (первыми «не знал») →
слова последнего урока → добор; до трёх открытых долгов ротацией по дню; кэп 20.
Курсор сессии и признак «очередь пройдена» живут в состоянии.

### Этап 7 — UI

Банк долгов знает пять состояний вместо двух: «закрыто» — это только `closed`.
Строка долга с кодом, счётом касаний и раскрывающимися примерами. События
разбора ИТОГа доходят до владельца уведомлением. На «Сегодня» — одна строка
«долги: N открытых · M на 1/2».

### Этап 8 — версия и деплой

`VERSION 2.7.0` в `state.js` и `sw.js`, `content/glossary.js` добавлен в кэш
service worker, `boot()` ловит смесь версий по API 2.7.0 и просит перезагрузить.

---

## Цифры миграции (копия живого экспорта, `study-v2-2026-09-02.json`)

| | было | стало |
|---|---|---|
| долгов всего | 43 | 43 (ни один не удалён физически) |
| открытых | 42 | **14** — письмо 7, математика 7 |
| на «1/2» | 9 | **6** (D-1, D-5, D-8, D-9, D-10, D-15) |
| закрытых | 1 | **2** |
| чек-лист языка | — | **13** |
| поглощённых | — | **13** |
| удалённых | — | **1** (D-31, ложное правило) |
| категорий с двумя открытыми | — | **нет** |
| банк слов | 99 | 96 (−3 выдуманных термина из B3.1) |
| дедлайны | без источника | content 16 · spread 36 · user 0 |
| примеры у открытых долгов | — | у всех 14, от 1 до 5 |
| серия | 46 | **46** |
| схема | 2 | 3 |

Повторный прогон миграции меняет ноль. Проверено также на трёх старых бэкапах
(23.08, 25.08 — схема 1; 01.09 — схема 2, там ещё жив D-7): падений нет,
идемпотентно, ремонт 2.6.5 и миграция 2.7.0 не мешают друг другу.

---

## Тесты

**Было 1099 зелёных → стало 1855.** Шесть новых наборов:

| набор | о чём |
|---|---|
| `cases.migrate-v3.js` | таксономия, ремонт банка, дедлайны, серия |
| `cases.summary-v3.js` | разбор ИТОГа v3 и разминка |
| `cases.content-v3.js` | глоссарий, задания, блок К |
| `cases.prompt-v3.js` | одиннадцать блоков промпта, ступень S0, ключи в конце |
| `cases.saturday.js` | правило «Суббота ⭐» |
| `cases.deck-v3.js` | колода дня: группы, кэп, ротация, курсор |
| `cases.ui-v3.js` | банк долгов группами, уведомления, строка на «Сегодня» |

Набор перестал зависеть от календаря: прогон на **24 датах 2026–2028** даёт
одинаковое число проверок и ноль провалов в любой день. До пакета набор краснел
два дня из семи и намертво с 08.09.2026.

---

## Деплой-чеклист для владельца

1. **Экспорт ДО обновления.** Настройки → «Скачать JSON». Без него откатиться
   некуда: миграция схемы 2 → 3 необратима.
2. Обновить **оба устройства**. На каждом — жёсткая перезагрузка (закрыть
   вкладку/приложение и открыть заново). Если приложение скажет «Загрузилась
   смесь версий» — перезагрузить ещё раз, это защита, а не поломка.
3. Проверить глазами:
   - в банке долгов **открыто 14 · на 1/2 — 6 · закрыто 2 · чек-лист 13**;
   - на экране «Программа» виден блок **«К»** с уроками **К.1 … К.10**;
   - промпт урока **Б7.1** содержит блок **[ГЛОССАРИЙ]**, а **[КЛЮЧИ]** стоят
     последними; слова «Погашено» в промпте нет;
   - в субботу **12.09** приложение выбирает урок **К.1** («суббота ⭐: задачи
     CEMC»);
   - кнопка **«Стало легко → S1»** на «Сегодня» **не показана** (для неё нужны
     три закрытых урока со средним счётом ≥ 8 за последние семь дней).
4. **Экспорт ПОСЛЕ обновления** — второй копией.

Чеклист прогнан автоматически на мигрированной копии живого экспорта: все
шестнадцать проверок сошлись.

---

## Отложено

- **Чек-лист радара в будни даёт очки.** `radar.js` рисует живой чек-лист в любой
  день недели, и полный чек-лист добавляет добавку `radar` (+1 очко «воскресного
  радара») хоть в среду. С 2.7.0 такой день **серию не держит** (`State.holdsStreak`
  проверяет воскресенье), но очки за него по-прежнему начисляются.
  Поведение досталось от 2.6.x, пакетом «Корень» не пересматривалось —
  **пересмотреть в 2.8**.

- **Категории долгов для дорожки `biz`.** В таксономии ТЗ 1.1 есть только `write`
  и `math`. Пока живы уроки Б5 (Ф0, дорожка `biz`), дорожка без своих категорий
  ведёт себя как `all`: код принимается любой, дорожка долга берётся из префикса
  (`П` → письмо, `М`/`О` → математика). Временное решение — **категории для `biz`
  к Ф2**, когда придёт BMI3C.

- **Раскладка ступеней S2–Г3.** ТЗ задало раскладку заданий только для S0 и S1.
  У остальных она считается тем же правилом: разогрев 2 · письмо 1 · стретч 2 ·
  остальное основа, от нижней границы диапазона заданий. До S2 ещё две нажатых
  кнопки — **уточнить, когда дойдёт**.

---

## Что придёт следом (только контент)

- **2.7.1** (≈14.09, после пакета №2 от учителей): опорные задания Б9, Б10 и
  К.4–К.6; возможный сдвиг дедлайнов Б10/Б14 под дату OSSLT — через
  `deadlineSource: 'content'`, без отдельной миграции.
- **2.7.2** (≈05.10): опорные задания Б11–Б16, К.7–К.10.

---

## Документы

- ТЗ пакета: `TZ-v2.7.0-koren.md` в корне репозитория.
- ТЗ-документ системы правился локально (`spec/study-system-v2-tz.md`, §7.2 —
  ступень S0 и переход по кнопке; §9 — формат пакета и урока v3, §9.3 — реальный
  состав Ф1). Папка `spec/` в `.gitignore` как личные материалы, поэтому правка в
  репозиторий не входит: обновлённый файл выгружен в
  `~/Downloads/study-system-v2-tz-v3.md`.
