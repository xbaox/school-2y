# REPORT 2.7.0 «Корень»

Черновик. Наполняется по ходу пакета, окончательный вид — этап 8
(что сделано по этапам, цифры миграции, список тестов, деплой-чеклист).

## Отложено

- **Чек-лист радара в будни даёт очки.** `radar.js` рисует живой чек-лист в любой
  день недели, и полный чек-лист добавляет добавку `radar` (+1 очко «воскресного
  радара») хоть в среду. С 2.7.0 такой день **серию не держит** (`State.holdsStreak`
  проверяет воскресенье), но очки за него по-прежнему начисляются.
  Поведение досталось от 2.6.x, пакетом «Корень» не пересматривалось —
  **пересмотреть в 2.8**.

- **Категории долгов для дорожки `biz`.** В таксономии ТЗ 1.1 есть только `write`
  и `math`. Пока живы уроки Б5 (Ф0, дорожка `biz`), дорожка без своих категорий
  ведёт себя как `all`: код принимается любой, дорожка долга берётся из префикса
  (`П` → письмо, `М`/`О` → математика). Временное решение — **категории для `biz`
  к Ф2**, когда придёт BMI3C.

## Документы

- ТЗ пакета: `TZ-v2.7.0-koren.md` в корне репозитория.
- ТЗ-документ системы правился локально (`spec/study-system-v2-tz.md`, §7.2 — ступень
  S0 и переход по кнопке; §9 — формат пакета и урока v3, §9.3 — реальный состав Ф1).
  Папка `spec/` в `.gitignore` как личные материалы, поэтому правка в репозиторий не
  входит: обновлённый файл выгружен в `~/Downloads/study-system-v2-tz-v3.md`.
