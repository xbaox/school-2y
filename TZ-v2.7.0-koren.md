# ТЗ v2.7.0 «Корень» — пакет механики + контент Фазы 1

**Для:** Claude Code (CC) в репозитории `school_2y`, ветка `main`, HEAD `c913fee` (VERSION 2.6.5).
**Автор:** Архитектор. **Владелец:** Бао. **Дата:** 02.09.2026. Дедлайн деплоя: **07.09**, крайний — **11.09** (в этот день SRS возвращает когорту из 55 слов).

## 0. Правила пакета

1. **Правило релея.** Если код противоречит этому ТЗ или ТЗ неполно — **остановись и доложи**, молчаливые обходы запрещены. Доклад = файл, строка, что именно противоречит, два варианта решения — Архитектор выбирает.
2. **Этап = коммит.** После каждого этапа: `npm test` (или текущий раннер) зелёный, отчёт по форме §9. Не начинать следующий этап без отчёта.
3. **Живые данные.** Этапы 1–2 меняют состояние пользователя. Работа только на копии экспорта из `~/Downloads/study-v2-2026-09-02.json` вне репозитория. Миграция обязана быть идемпотентной (повторный запуск ничего не меняет) и печатать в консоль сводку «было → стало».
4. **VERSION** поднимается **один раз**, в этапе 8 (`sw.js` и где ещё читается), на 2.7.0.
5. Никаких новых зависимостей. Vanilla JS, как весь проект.
6. Терминология в UI и промптах: без слов «пресет», «юнит», «водопад», «квиз». Каждый код курса и акроним в текстах для ученика — с расшифровкой.
7. Файлы контента приходят готовыми от Архитектора: `content/phase1.js`, `content/glossary.js`. Их **не редактировать**, только подключить и покрыть тестами; несовместимость — доклад по п. 1.

**Корень, который убивает пакет:** ИИ-преподаватель сочинял то, что должно быть зафиксировано (определения, критерии, задания, идентичность и закрытие долгов), а приложение верило его словам. После пакета: **контент и приложение — источник истины, ИИ — исполнитель.** Каждый этап ниже закрывает одну дыру.

---

## Этап 1. Данные: таксономия долгов, ремонт банка, дедлайны, серия

### 1.1. Таксономия категорий (закрытый список) — новый модуль `debts-cats.js` (или внутри `state.js`)

```js
var DEBT_CATS = [
  // дорожка write — «Письмо и чтение»
  { code: 'П1',  track: 'write', name: 'Point — спорное утверждение с глаголом, не тема и не цель' },
  { code: 'П2',  track: 'write', name: 'Evidence — проверяемый факт: прошедшее время, число, источник' },
  { code: 'П3',  track: 'write', name: 'Explain — разбор именно Evidence («this shows that…»), не пересказ Point' },
  { code: 'П4',  track: 'write', name: 'Link — возврат к Point без обобщений и новых фактов' },
  { code: 'П5',  track: 'write', name: 'Главная мысль текста — предложение с глаголом, накрывающее весь текст' },
  { code: 'П6',  track: 'write', name: 'Полные предложения — без ярлыков, фрагментов после двоеточия, голых формул' },
  { code: 'П7',  track: 'write', name: 'Баллы → объём: число идей и предложений = число marks; формат под команду' },
  { code: 'П8',  track: 'write', name: 'Команда — ответ строго под command word (state / define / explain / assess…)' },
  { code: 'П9',  track: 'write', name: 'Весь вопрос — все части и все требования (длина, язык, форма)' },
  { code: 'П10', track: 'write', name: 'Вывод из текста (inference) — опирается на конкретную строку текста' },
  // дорожка math — «Математика»
  { code: 'М1',  track: 'math',  name: 'Требования условия — метод, язык ответа, форма («без цифр»), конкретный вопрос' },
  { code: 'М2',  track: 'math',  name: 'Ходы записаны, запись читаема — иначе теряются part marks' },
  { code: 'М3',  track: 'math',  name: 'Форма ответа — пара (x, y), единицы, знак, округление как просили' },
  { code: 'М4',  track: 'math',  name: 'Параметры словами — m и b: знак, единицы, смысл («m = −4 L per hour, which is the rate…»)' },
  { code: 'М5',  track: 'math',  name: 'Объяснение решения — одно лицо, present simple, полные предложения, финал называет величину' },
  { code: 'М6',  track: 'math',  name: 'Проверка ответа по условию — домен, здравый смысл, подстановка' },
  { code: 'М7',  track: 'math',  name: 'Определение термина (define) — категория + отличие, не пример и не число' },
  { code: 'М8',  track: 'math',  name: 'Полнота — все части вопроса, вторая половина не брошена' },
  { code: 'О1',  track: 'math',  name: 'Вход — после видео задан свой вопрос по материалу' }
];
```

Правила таксономии (реализуются в этапе 2, данные готовятся здесь):
- У долга новое поле `cat` (код из списка). Долг без валидного `cat` создать нельзя.
- **В одной категории — не больше одного открытого долга.** Это структурно убивает дубли.
- Дорожка `all` (Б16): категории обеих дорожек.
- Список редактируется только Архитектором через ТЗ, не из UI.

### 1.2. Ремонт банка долгов — миграция `meta.version 2 → 3` (в `state.js`, рядом с `P0_DEADLINES`, `state.js:26–30`, применение по образцу `state.js:238–242`)

Экспорт содержит 43 долга: 42 открытых, D-4 закрыт. Правила слияния — как утверждено (остаётся старейший `did`; `clearedIn` объединяются по уникальным урокам; если объединение даёт два разных урока — долг закрывается честно; в поле `text` остаётся канонический текст ниже; тексты поглощённых долгов уходят в `examples`).

| Остаётся | `cat` | Поглощает | Канонический `text` | `clearedIn` после |
|---|---|---|---|---|
| D-1 | П7 | D-2 | Баллы → объём: число идей и предложений не совпадает с числом marks; формат ответа не переносится с одной команды на другую | [B1.4] |
| D-10 | П2 | — | Evidence не факт: будущее время / без числа / без источника («I can buy» вместо «Last month I sold 14…») | [B1.4] |
| D-11 | П3 | D-43 | Explain не разбирает Evidence: после факта нет «this shows that…», объясняется Point | [] |
| D-12 | П4 | — | Link уходит в обобщение или добавляет новый факт вместо возврата к Point | [] |
| D-13 | П1 | D-37 | Point — тема или цель вместо спорного утверждения с глаголом | [] |
| D-44 | П5 | — | Главная мысль записана как тема без глагола | [] |
| D-25 | П6 | D-30, D-35, D-42 | Ярлык, фрагмент после двоеточия или голая формула вместо полного предложения | [] — **track → `write`** |
| D-5 | М4 | D-6, D-24 | m и b словами: теряется знак, единицы или смысл (нужно: m = −4 L per hour, which is the rate…) | [B2.4] |
| D-8 | М1 | D-16, D-22, D-28, D-33 | Требования условия не выполнены: метод / язык ответа / «без цифр» / конкретный вопрос | [B2.4] |
| D-15 | М3 | — | Ответ системы одним числом вместо пары (x, y); форма ответа не как просили | [B2.4] |
| D-17 | М5 | — | В объяснении решения плавает лицо и время (I would → Add → We'll) | [] |
| D-19 | М2 | D-23 | Ходы не записаны или запись нечитаема (450 → 449) — потеря part marks | [] |
| D-21 | М8 | — | Отвечает на первую половину вопроса, вторую бросает | [] |
| D-9 | О1 | — | После видео не задан свой вопрос по материалу | [B2.4] |

Дополнительно:
- **Удалить D-31** («артикль после is called: the вместо a») — ложный долг, правило выдумано; `status: 'deleted'`, `reason: 'ложное правило'`, из всех счётчиков и колоды исключён.
- **Закрыть D-3** («state и define») — `status: 'closed'`, `closedDate: '2026-09-02'`, `note: 'B1.4 + три разминки 30.08–02.09'`.
- **D-4** остаётся закрытым.
- **Чек-лист языка** (не долги): D-14, D-18, D-20, D-26, D-27, D-29, D-32, D-34, D-36, D-38, D-39, D-40, D-41 → `status: 'checklist'`. Исключены из открытых, из колоды карточек, из инъекции в промпт; в списке долгов показываются отдельной свёрнутой группой «Ушли в чек-лист языка».
- Поглощённые: D-2, D-6, D-16, D-22, D-23, D-24, D-28, D-30, D-33, D-35, D-37, D-42, D-43 → `status: 'merged'`, `mergedInto: 'D-n'`.
- Новые поля долга: `cat`, `examples: [{lesson, text, date}]`, `failedIn: []`, `lastInjected: null`, `shownCount: 0`.
- Ремонт слов: удалить карточки-мусор из `srs`/банка слов: `coverage test`, `approximate fact`, `conclusion drawn from` (выдуманные или бессмысленные термины из B3.1).

**Контроль после миграции (зафиксировать тестом на копии экспорта):** открытых долгов **14** (write 7: D-1, D-10, D-11, D-12, D-13, D-25, D-44; math 7: D-5, D-8, D-9, D-15, D-17, D-19, D-21); на «1/2» — **6** (D-1, D-10, D-5, D-8, D-15, D-9); closed 2 (D-3, D-4); checklist 13; merged 13; deleted 1; ни одна категория не имеет двух открытых.

### 1.3. Дедлайны: источник вместо «только в пустое» (`state.js:544–548`, `upsert`)

Сейчас контент никогда не перепишет существующий дедлайн — новые пакеты контента до пользователя не доезжают. Заменить правило:
- Новое поле блока в состоянии: `deadlineSource: 'content' | 'user' | 'shift'`.
- `upsert`: если `deadlineSource !== 'user' && deadlineSource !== 'shift'` и `meta.deadline` отличается — **перезаписать** и поставить `'content'`.
- `State.setDeadline` → `'user'`; `State.shiftPhase` → `'shift'`.
- Миграция v3: для каждого блока — если дедлайн совпадает с контентным (или с `P0_DEADLINES` для Б1–Б6) → `'content'`; иначе, если дедлайн есть → `'user'`.
- `LATER_BLOCKS` (`state.js:57–96`) и `spreadDeadlines` не трогать.

### 1.4. Серия: лазейка закрыта

День считается **пустым** для серии, если в нём нет ни закрытого урока, ни выполненной минималки (`minimalSteps` не все `true`). Добавки (Проект, Клуб, Тест, Радар, Доп. урок) дают очки, но **серию не держат**. Радар-день (воскресенье) с выполненным радаром — не пустой (доктрина). Тест: день с `addons: ['project']`, без урока и без минималки → пустой.

**Приёмка этапа 1:** миграция на копии экспорта даёт цифры из 1.2; повторный запуск — нулевые изменения; тесты зелёные; сводка миграции в отчёте.

---

## Этап 2. Парсер ИТОГа и разминки (`state.js` — `applySummary` и парсер, `~state.js:1187`)

### 2.1. Шаблон ИТОГа v3 (то, что печатает `prompts.js`, парсит `state.js`)

```
=== ИТОГ УРОКА B7.1 ===
Пройдено: …
Уровень: L2
Счёт: 8/10
Слова (6–8): слово — перевод; слово — перевод; …
Долги: П3 — короткий пример ошибки (задание 4)
М2 — короткий пример ошибки
Засчитано: [D-10]; [D-5]
Чек-лист: 1✓ 2✓ 3✗ 4✓ 5✓
В разогрев: вопрос; вопрос; вопрос
Письмо: …
=== КОНЕЦ ===
```

### 2.2. Правила разбора

- **Сплит** полей «Долги» и «Засчитано» только по переводам строк (`;` остаётся разделителем для «Слова», «Засчитано» и «В разогрев» — как утверждено ранее).
- **Долги.** Каждая строка обязана начинаться с кода категории: `/^(П\d{1,2}|М\d|О\d)\s*[—–-]\s*(.+)$/`. Код должен принадлежать дорожке урока (для `all` — любой). Строка без валидного кода **отбрасывается** с уведомлением «долг без категории отброшен: …» (ИИ не может создавать долги вне таксономии — это и есть корень).
  - Категория **уже имеет открытый долг** → это **повтор**, не новый долг: `clearedIn = []` (прогресс сброшен), `failedIn.push(lessonId)`, `examples.push({lesson, text, date})`, уведомление «повтор D-n (П3) — прогресс сброшен».
  - Категория свободна → **новый долг** `{ did: следующий D-n, cat, text: name категории + ' — ' + пример, track: дорожка урока (для all — по префиксу кода: П → write, М/О → math), createdIn, clearedIn: [], examples: [первый пример] }`.
  - **Кэп ≤3 новых** за урок (жёсткий, в `applySummary`), отрезанные — уведомление.
- **Засчитано** (алиас `Погашено` принимать ещё два релиза): id засчитывается только если он был в `injected.lessons[lessonId]` (правило 2.6.5 сохраняется), долг открыт и **не** отмечен повтором в этом же ИТОГе (повтор побеждает). Добавить урок в `clearedIn`, если его там нет. Два разных урока в `clearedIn` → `status: 'closed'`, `closedDate`. Ни при каких словах ИИ («закрыт», «погашен») приложение не закрывает долг иначе как этим правилом.
- **Чек-лист** (опционально): `Чек-лист: 1✓ 2✓ 3✗ 4✓ 5✓` → `state.checklist.stats[i] = {clean, total}` (пять пунктов, тексты живут в `prompts.js`, §4.3). Строки нет — ничего не пишем.
- **Слова 6–8** — парсер не режет, контракт требует.
- Уведомления после вставки (UI — этап 7, здесь только события): список строк вида `D-15: 2/2 → закрыт`, `D-10: 1/2`, `повтор D-11 (П3) — прогресс сброшен`, `новый долг D-45 (М6)`, `отброшено: …`.

### 2.3. Разминка пишет в приложение

Поле «Вставить итог» принимает строку с префиксом `РАЗМИНКА:` — например `РАЗМИНКА: D-1 ✓ D-10 ✗ · слова 15/15`.
- `✓` — **только первое касание**: если `clearedIn` пуст и id есть в `injected.min` → `clearedIn = ['warmup:' + date]`. Если у долга уже есть касание — ничего (закрывающее касание — только урок).
- `✗` — записывается в `failedIn` с пометкой `warmup`, прогресс **не** сбрасывается.
- Слова из разминки не трогаем (SRS ведёт карточки сам).
- Обратно совместимо: строка `warmup:2026-09-03` в `clearedIn` считается одним «уроком» для правила двух разных уроков.

**Приёмка этапа 2:** тесты на все ветки 2.2–2.3, включая: строка без кода отброшена; повтор сбрасывает прогресс; кэп 3; `Погашено` как алиас; закрытие только при двух разных `clearedIn`; разминочное ✓ не закрывает.

---

## Этап 3. Контент: реестр, формат v3, drop-in файлов

### 3.1. Реестр (`content/registry.js`)

- Новый метод `CONTENT.registerGlossary(obj)` (или пакет с `glossary`), хранилище `CONTENT.glossary` — объект `ключ → {en, ru, def, ex, non}`. Файл `content/glossary.js` подключить в `index.html` **до** пакетов фаз; очередь `__CONTENT_Q` как у пакетов.
- Поле блока `label` (необязательное): `State.blockLabel(id)` возвращает `label`, если он есть, иначе `'Б' + num`. Уроки такого блока подписываются `label + '.' + n` (например `К.1`).
- Поля урока (все существующие остаются): `terms` (массив строк-ключей глоссария **или** инлайн-объектов `{en, ru, def, ex, non}`), `text` (строка или `null`), `tasks` (массив или `null`), `type: 'contest'`. Реестр их не трогает, только пропускает; `CONTENT.lesson(id)` отдаёт как есть.
- Формат `tasks[i]`: `{ q, ru, marks, level: 'L1'|'L2'|'L3', key, probe }` — `probe` необязательный код категории долга, который это задание проверяет. Для `type: 'contest'`: `{ part: 'A'|'B', q, ru, marks, key }`.
- Пакет без `phase` по-прежнему отбрасывается.

### 3.2. Подключение файлов Архитектора

- Заменить `content/phase1.js` файлом из пакета целиком. Добавить `content/glossary.js`.
- Блок **B12** меняет `track: 'biz' → 'write'` и заголовок (контент главнее — зеркало обновится само, миграция не нужна). Блок **B53** новый (`label: 'К'`, `deadline: null`, 10 уроков `type: 'contest'`).
- Тесты `tests/cases.skipped.js:148–163`: обновить залоченные заголовки Б7–Б16 по новому файлу; строка 163 (`blockLessons('B7').length === 0`) → 4. Новые тесты: Б7 и Б8 имеют по 4 урока с `terms`, `tasks` (5 заданий: 4 уровня L2 или ниже + 1 L3) и ненулевым `writing`; у Б8 каждый урок имеет `text`; B53 имеет 10 уроков `type: 'contest'`, у К.1–К.3 по 3 задания с `key`; `CONTENT.glossary` содержит ≥ 40 ключей; все строковые `terms` во всех уроках Ф1 существуют в глоссарии.
- `tests/cases.skipped.js:166–191` (мутация `type` на живом объекте) — переписать на реальный урок `B53.1`, убрать мутацию.

### 3.3. ТЗ-документ

Если в репозитории есть `study-system-v2-tz.md` (или `docs/…`): обновить §9.2 (формат контента — поля 3.1) и §7.2 (ступень S0 из этапа 4). Если файла нет — сообщить, Архитектор внесёт правки в копию владельца.

**Приёмка этапа 3:** приложение грузится с новым контентом; тесты 3.2 зелёные; в «Программе» видны Б7–Б16 с уроками и блок «К» с подписью уроков К.1…К.10.

---

## Этап 4. Промпты (`prompts.js`): контракт v3, новые блоки, ступень S0, разминка, конкурс

### 4.1. Контракт преподавателя v3 — заменяет v2 целиком

Перед заменой **вставить в отчёт текст v2 verbatim** и перечислить всё из v2, чего нет в v3 (если найдёшь — доклад по правилу релея, не молчаливое добавление).

```
КОНТРАКТ ПРЕПОДАВАТЕЛЯ v3
1. Роль и язык. Ты — преподаватель школьной программы Онтарио для ученика, чей домашний язык русский. Инструкции и разборы — по-русски или коротко по-английски с полной русской строкой ниже. Под каждым английским предложением-образцом — русская строка, не сокращённый пересказ.
2. Формат урока — по ступени из [КОНТЕКСТ]. Строго одно задание на сообщение. Заголовок каждого задания: «Задание N/8 · L2 · 2 marks». Два спринта с паузой 2–3 минуты.
3. Стоп-слово «стоп» / «устал» → сразу ИТОГ, урок засчитан, без уговоров. Признаки усталости или раздражения (короткие ответы, ругань) → один раз предложить паузу или стоп, без расспросов.
4. Опорные задания из [ОПОРНЫЕ ЗАДАНИЯ] обязательны и идут в своём порядке; условие менять нельзя. Разогрев (2 задания) — из [РАЗОГРЕВ] и долгов с пометкой ПРИОРИТЕТ. Сочинять задания самому — только если опорных нет.
5. Попытка ученика — всегда первой: не подсказывать ход и не давать ответ до его попытки. Застрял → одна опора-вопрос, не решение.
6. Проверка — как экзаменатор Онтарио: part marks по [КЛЮЧИ]; каждый снятый балл объяснён одной строкой EN + RU. Ключ до попытки не цитировать и не пересказывать.
7. Образец до критики: перед разбором ошибки — решённый параллельный пример (другая тема, тот же приём).
8. Одна правка на ответ: разбирается только главная ошибка по цели задания; остальное — одной строкой «Мелочи: …» без разбора. На верный ответ — не больше одной мелочи. Похвала только конкретная («Evidence в прошедшем с числом — стандарт 4/4»); слова «молодец», «отлично», «супер» не использовать.
9. При серьёзной ошибке — одна фраза стандарта: «Стандарт высокий, ты ему соответствуешь; вот один шаг: …» — и сам шаг.
10. Успех около 80 %: два ответа подряд ниже половины баллов → следующее задание уровнем ниже или с опорой. L3 — только задание ⭐ и по желанию; пропуск ⭐ баллы не снимает.
11. Термины и определения — только из [ГЛОССАРИЙ] и терминологии класса Онтарио, дословно. Новое понятие вводится так: термин → категория и отличие → пример → не-пример → русская строка. Свои названия приёмов не выдумывать. Критерий оценки называется до задания и в уроке не меняется.
12. Форматы заданий — ротация: реши · найди ошибку (я даю решение с ошибкой) · объясни как учитель · оцени как экзаменатор по критериям · опровергни (я утверждаю неверное) · 60 секунд на время. Не больше двух одного формата подряд; мета-задание («сколько предложений и почему») — не больше одного за урок.
13. Математика — на бумаге, фото в чат; набранный текст принимается только с промежуточными шагами. Ходы без записи — снятие балла (М2).
14. Перед письменным заданием напечатать [ЧЕК-ЛИСТ ЯЗЫКА]. Ошибки чек-листа — не долги: отмечаются в строке «Чек-лист» ИТОГа и одной строкой «Мелочи».
15. Долги решает приложение. В ИТОГе: «Засчитано: [D-…]» — только за верную демонстрацию долга из [ДОЛГИ] без подсказки; «Долги: П3 — пример» — только с кодом категории из [ДОЛГИ], не больше трёх; если категория уже открыта — это повтор, не новый долг. Слов «закрыт», «погашен» не писать. За урок проверить не меньше двух долгов с пометкой ПРИОРИТЕТ.
16. Визуалы (таблицы, схемы, графики) строит преподаватель; ученик не рисует ради иллюстрации.
17. Финал: «Что взял» — три конкретных пункта; «Связка» — одно предложение к школе этой недели; счёт N/10; без нотаций.
18. ИТОГ — строго по шаблону из [ИТОГ], все строки; слова 6–8, из них не больше двух понятий; долги с кодами; «Засчитано» по id. Перед ИТОГом самопроверка: коды есть, слова «закрыт» нет, долгов ≤3, слов 6–8.
19. После ИТОГа — одна строка про отдых («5–10 минут без экрана»), больше ничего.
20. Приоритеты: сон > школа > урок. Если ученик пишет, что поздно или завтра тест — предложить минималку и закончить.
```

### 4.2. Порядок блоков промпта урока

1. Роль + контракт v3.
2. `[КОНТЕКСТ]`: фаза · блок · урок · дорожка · цель · **ступень** (S0: «8 заданий = 2 разогрев L1 · 4 основа L2 · 1 письмо · 1 стретч ⭐ L3 по желанию; спринты ~13–15 минут») · прошлый раз (как сейчас).
3. `[ГЛОССАРИЙ]` — из `lesson.terms` (ключи разворачиваются из `CONTENT.glossary`, инлайн — как есть): `en — def. Example: ex. Not: non. RU: ru`. Строка «использовать дословно».
4. `[ТЕКСТ]` — `lesson.text`, если есть.
5. `[ОПОРНЫЕ ЗАДАНИЯ]` — из `lesson.tasks`: «Основа 1–4» (уровни ≤ L2) и «Стретч ⭐» (L3); формат `n. (L2, 2 marks) q  /  RU: ru  /  проверяет: probe`. **Без ключей.** Если `tasks === null` — строка «опорные задания придут следующим пакетом контента; вести по фокусу, 4 основных задания».
6. `[ДОЛГИ дорожки]` — **все** открытые долги дорожки (для `all` — обеих), по строке на категорию таксономии: `П2 Evidence — … · ОТКРЫТ D-10 · 1/2 · последний пример: «…» (B1.2) ← ПРИОРИТЕТ` / `П8 Команда — … · чисто`. Пометку **ПРИОРИТЕТ** получают до трёх долгов: сначала с «1/2», затем давно не показывавшиеся (`lastInjected`). Заменяет окно из пяти FIFO. При копировании промпта писать `lastInjected` и `shownCount` каждого показанного долга, а также `injected.lessons[lessonId]` (как сейчас).
7. `[ЧЕК-ЛИСТ ЯЗЫКА]` — пять строк из 4.3.
8. `[РАЗОГРЕВ]` — «В разогрев» из ИТОГа прошлого урока дорожки + слова прошлого урока (как сейчас).
9. `[ЭТАПЫ]` — `stagesBlock()` по ступени.
10. `[ИТОГ]` — шаблон v3 из 2.1 с реальным id урока.
11. `[КЛЮЧИ]` — в самом конце: `Основа 1: key`, …, `Стретч: key`, `Письмо: критерии`. Заголовок: «=== КЛЮЧИ — для преподавателя; ученик не читает ===».

`lesson.title` без фолбэка — оставить как есть, но добавить тест, что у всех уроков всех фаз есть `title`.

### 4.3. Чек-лист языка (текст в `prompts.js`, пять пунктов, номера фиксированы — по ним считается статистика)

```
1. Деньги и числа: знак валюты перед числом и запятая в тысячах — $1,200; десятичная точка — 33.3.
2. Артикли: the перед конкретной величиной (the total cost, the y-axis); перед названием метода артикля нет (by elimination).
3. Because: часть с because не стоит отдельным предложением — приклей к главному («…is an opinion because…») или начни с Because и закончи главным; запятая перед because обычно не нужна.
4. Два предложения не склеиваются запятой (comma splice): точка, точка с запятой или союз.
5. Глагол: -s у he / she / it / the business (gives); объект после переходного глагола (sell them); наречие частоты после be (phones are usually).
```

### 4.4. Ступень S0 «Старт школы» и переход

- Таблица ступеней получает `S0: { name: 'Старт школы', minutes: 30, tasks: 8, layout: '2 разогрев L1 · 4 основа L2 · 1 письмо · 1 стретч ⭐' }`. S1 = 35 минут, 12 заданий: `2 · 7 · 1 · 2 ⭐`. S2–S4, Г1–Г3 — без изменений.
- Состояние: `state.scale = { stage: 'S0', since: '2026-09-08' }` — по умолчанию для фазы p1 (миграция v3 ставит S0, если ступень не задана). **Автоперехода нет.**
- На экране «Сегодня» кнопка **«Стало легко → S1»** появляется, когда средний счёт последних 7 дней ≥ 8 при ≥ 3 закрытых уроках; нажатие → `stage: 'S1'`, `since: today`, кнопка исчезает; дальше — существующая логика шкалы, но тоже только по кнопке («→ S2» и т. д. по тому же условию).
- Разгон Ф0 (30–35′/10–12 до 07.09) — как есть.

### 4.5. Промпт разминки (минималка)

- Долги в разминку: до трёх — сначала «1/2», затем давно не показывавшиеся; писать `injected.min` (как сейчас).
- Дополнить: «Определения — только дословно из строки долга. Слов "закрыт / погашен" не писать. Последняя строка ответа — одна: `РАЗМИНКА: D-1 ✓ D-10 ✗ · слова 15/15`; ученик вставит только её».

### 4.6. Конкурсный урок (`prompts.js:225–264`)

Оставить контракт `type: 'contest'` (qRange 3, без видео и письма). Дополнить: печатать `[ЗАДАЧИ]` из `lesson.tasks` (часть A — короткий ответ, часть B — полное решение с записью ходов) и `[КЛЮЧИ]` в конце; правила 5–7, 13, 15 контракта действуют; ИТОГ — тот же шаблон, «Письмо: —».

**Приёмка этапа 4:** снапшот-тест промпта для `B7.1` (все 11 блоков в этом порядке, ключи только в конце, ни одного «Погашено» в шаблоне, ступень S0), для `B53.1` (конкурсный) и для разминки; тест кнопки «Стало легко».

---

## Этап 5. Выбор урока (`waterfall.js`)

- **`ruleSaturday`** — новый элемент `RULES[0]` (перед `ruleDeadline`): если сегодня суббота (по логическому «сегодня» с границей 04:00) и в текущей фазе есть незакрытый урок `type: 'contest'` → вернуть `{ track, blockId, lessonId, reason: { kind: 'contest', text: 'суббота ⭐: задачи CEMC' } }`. Первый незакрытый по порядку. Если конкурсных нет — правило молчит, дальше как сейчас.
- `EXPLAIN` (`waterfall.js:356`) — добавить строку правила 0 «Суббота ⭐», перенумеровать, обновить шапку модуля (`waterfall.js:5–8`) и переставить устаревший комментарий `waterfall.js:86`.
- `ruleDeadline`: блок с `deadline === null` пропускается — как сейчас; `CONTENT.num` на `waterfall.js:102` — добавить гвард `window.CONTENT` для единообразия.
- `nextLessonInTrack` не трогаем: B53 сортируется последним, конкурсные уроки в будни не всплывут, пока есть обычные.

**Приёмка этапа 5:** тесты: суббота + незакрытый К → К.1; суббота без конкурсных → прежний выбор; воскресенье — как сейчас; будни — как сейчас.

---

## Этап 6. Колода дня (утверждено ранее, без изменений)

Подошедшие повторы + в работе (последний «не знал» — первыми) + слова последнего урока + ≤3 открытых долга ротацией по дню; кэп ~20; шафл внутри групп с сидом от даты; признак «очередь пройдена — сессия закрыта» с курсором в состоянии. Правка лид-текстов `journal.js:66`, `app.js:476` — убрать обещание «колода сокращается». Тесты `cases.srs.js`, `cases.cards-count.js` обновить. Долги в колоду — только `status: 'open'`.

---

## Этап 7. UI: индикатор долгов, уведомления, чек-лист

- Экран долгов: заголовок «открыто 14 · на 1/2 — 6 · закрыто 2 · чек-лист 13». Каждый долг: `D-10 · П2 Evidence` · текст · «касаний 1/2 · последнее B1.4 (01.09) · показан 3 раза» · раскрывающийся список примеров. Группы: открытые (сначала «1/2»), закрытые, «ушли в чек-лист», поглощённые (свёрнуто).
- После вставки ИТОГа / РАЗМИНКИ — уведомление со строками из 2.2 (по одной строке на событие, 4–6 секунд или до тапа).
- Экран «Сегодня»: одна строка «долги: 14 открытых · 6 на 1/2» под блоком урока (**не** на карточке колоды — тот индикатор отменён владельцем, не возвращать).
- Чек-лист: в разделе слов/долгов строка «чек-лист: 1 — 4/5 чисто · 2 — 3/5 · …» по `state.checklist.stats`.

---

## Этап 8. Версия, документация, деплой

- VERSION → **2.7.0** во всех местах (`sw.js` обязательно); `boot()` — защита от смеси версий как в 2.6.2.
- `REPORT-2.7.0.md`: что сделано по этапам, цифры миграции, список тестов (было 1098 → стало N).
- Деплой-чеклист для владельца (в отчёте): экспорт **до** обновления → обновить оба устройства → проверить: открытых долгов 14, блок «К» виден, промпт B7.1 содержит [ГЛОССАРИЙ] и [КЛЮЧИ] в конце, суббота 12.09 выбирает К.1, кнопка «Стало легко» **не** показана → экспорт **после**.

---

## 9. Форма отчёта по этапу

```
ЭТАП N — готово / стоп
Файлы: …
Тесты: было X зелёных → стало Y (новых: список имён кейсов)
Миграция (этапы 1–2): было → стало (цифры)
Противоречия ТЗ ↔ код: нет / список (файл:строка, суть, два варианта)
Вопросы Архитектору: …
```

## 10. Что придёт следом (не в этом пакете, только контент)

- 2.7.1 (≈14.09, после пакета №2 от учителей): `content/phase1.js` с опорными заданиями Б9, Б10 и К.4–К.6; возможный сдвиг дедлайнов Б10/Б14 под дату OSSLT — через `deadlineSource: 'content'` (этап 1.3), без отдельной миграции.
- 2.7.2 (≈05.10): опорные задания Б11–Б16, К.7–К.10.
