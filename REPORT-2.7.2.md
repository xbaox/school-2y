# REPORT 2.7.2 + 2.7.3 — закрытие отложенного

Пакет из двух этапов поверх «Корня»: `2.7.1 → 2.7.2 → 2.7.3`.
Этап 1 (2.7.2) уже на боевом — он ставился в день выпуска, чтобы владелец увидел
карточку вопросов утром 8.09. Этап 2 (2.7.3) на боевом с 07.09.2026.

**Что закрыто:** все пункты раздела «Отложено» отчёта 2.7.0 — радар в будни,
категории долгов для дорожки «Бизнес», раскладка ступеней S2–Г3, — плюс два
наблюдения владельца (слова прошлого урока, перевод «to explain») и новая
механика урока по школьному домашнему заданию.

---

## Этап 1 → VERSION 2.7.2 (на боевом с 07.09.2026)

### 1.1 Карточка «Вопросы в школе»

Новый тип события радара `questions`: шесть вопросов к консультанту и учителю
математики, английский текст крупно, русская строка под ним, чекбокс и поле
«ответ одной строкой». Сеется один раз при загрузке (`Radar.seedQuestions`),
на «Сегодня» держится с даты события, пока не отмечены все пункты, но не дольше
`QUESTIONS_UNTIL = '2026-09-11'`; в «Радаре» — всегда.

Ответ пишется на месте: к вечеру он забыт, а вопросы задаются один раз.

### 1.2 Слова прошлого урока — вариант Б

`lessonWords` в промпте больше не пустеет, когда все слова последнего урока
выучены: `lastLessonWords` отступает по урокам **той же дорожки** назад
(до 12 итогов), пока не наберётся до `WARM_WORDS = 5` невыученных.

### 1.3 Перевод карточки «to explain»

RU-строка стала «показать, как или почему, по шагам» — по формулировке самого
глоссария. Правка идёт через `WORD_FIXES` в `state.js`, безусловно и
идемпотентно, по точному совпадению старого текста; `content/glossary.js` не
тронут.

### 1.4 Радар — только по воскресеньям

`Radar.radarDay(iso)` закрывает `toggleCheck` и `setChecklistAll` в будни, а
чекбоксы приходят `disabled`. Добавка `radar` и её очко достаются только
воскресенью — как и требует доктрина: серию будний радар не держал и раньше,
но очки начислял.

---

## Этап 2 → VERSION 2.7.3 (на боевом)

### 2.1 Урок по домашнему заданию школы

**Состояние.** `state.hw[id] = { date, course, track, score, level }` — отдельное
хранилище. Программный урок разбором школьного ДЗ пройденным **не становится**:
`s.lessons` ДЗ не касается, `refreshBlockDone` для него не зовётся.

**Курсы.** `settings.schoolCourses` — четыре курса с расшифровкой и дорожкой:

| код | что это | дорожка |
| --- | --- | --- |
| MHF4U | Advanced Functions — продвинутые функции, 12 класс | математика |
| ENG2D | English — английский 10 класса, академический | письмо |
| GLC2O | Career Studies — карьера и планирование | бизнес |
| ICS3U | Computer Science онлайн — информатика, e-learning | информатика |

Код `ICS3U` помечен `editable: true`: до разговора с консультантом он
неизвестен, это предположение. Отдельного экрана настройки курсов нет и не
планируется — код заменяется точечной правкой дефолта и значения в
`settings.schoolCourses` в 2.7.4, по ответу консультанта.

**Кнопка.** «Урок по ДЗ» на «Сегодня», понедельник–пятница, пока норма дня не
закрыта. Счётчик «ДЗ-уроков на неделе: n/3», неделя с понедельника, при трёх
кнопка неактивна и объясняет почему. Выбор курса → `id = HW-ГГГГ-ММ-ДД-дорожка`,
`day.hw` и `day.hwCourse` помечены; `day.pick` и «Дальше» не трогаются —
программный урок остаётся на следующий день.

**Промпт.** `PROMPTS.lesson('HW-…')` собирается тем же `lessonPrompt` с заменами:
в `[КОНТЕКСТ]` строка «Формат: урок по домашнему заданию школы — курс MHF4U
(Advanced Functions — продвинутые функции, 12 класс)»; вместо `[ТЕКСТ]` и
`[ОПОРНЫЕ ЗАДАНИЯ]` — блок `[ДЗ]`; `[КЛЮЧИ]` нет вовсе (ключей у школьного ДЗ не
существует, они в голове преподавателя). `[ГЛОССАРИЙ]` приходит запасной по
дорожке, `[ДОЛГИ]` — доска дорожки, `[ЭТАПЫ]` и `[ФИНАЛ]` — как у ступени, id в
ИТОГе `HW-…`.

**Стретч на ДЗ-уроке — всегда «по желанию».** `hwParams` снимает
`stretchRequired` на любой ступени, включая Г1–Г3, и убирает клаузу «стретч ⭐⭐
обязателен» из «Особого»: к школьному ДЗ конкурсной задачи по теме может просто
не найтись, и обязательность толкала бы преподавателя сочинять её из ничего.
Обязательным ⭐⭐ остаётся только в программных уроках.

**Критерии письма.** В конце промпта ДЗ-урока стоит блок
`=== КРИТЕРИИ ПИСЬМА ===` с той же строкой из трёх баллов по дорожке, что
печатается в `[КЛЮЧИ]` обычного урока (`WRITING_KEY[trackId]`, для `biz` и `cs` —
запасная строка письма, как и там). Без неё три балла за письмо ставились бы на
глаз, а счёт урока из них складывается.

**Разбор ИТОГа.** `day.lessons` получает id (норма дня закрыта), свежесть дорожки
сброшена, `stats.lessonsDone` растёт, долги, слова, чек-лист и стретч работают
как обычно. `lessonTrack('HW-…')` читает дорожку из суффикса id — иначе «Прошлый
раз» и доска долгов ДЗ-урока не видят.

### 2.2 Категории долгов для дорожки «Бизнес»

`DEBT_CATS` вырос с 19 до 24: Б1 регистр · Б2 структура ответа · Б3 термины
точно · Б4 число в аргументе · Б5 питч. Парсер принимает `Б\d`, латинская `B`
исправляется на кириллическую в `normCode`. Фолбэк «дорожка без своих категорий
ведёт себя как all» остался только у `cs`.

### 2.3 Раскладка ступеней целиком

| ступень | время | заданий | разогрев | основа | письмо | стретч |
| --- | --- | --- | --- | --- | --- | --- |
| S0 | 30′ | 8 | 2 | 4 | 1 | 1 ⭐ |
| S1 | 35′ | 12 | 2 | 7 | 1 | 2 ⭐⭐ |
| S2 | 40′ | 14 | 2 | 9 | 1 | 2 ⭐⭐ |
| S3 | 45′ | 16 | 2 | 11 | 1 | 2 ⭐⭐ |
| S4 | 50′ | 18 | 2 | 13 | 1 | 2 ⭐⭐ |
| Г1 | 50′ | 18 | 2 | 13 | 1 | 2 ⭐⭐ |
| Г2 | 50′ | 18 | 2 | 13 | 1 | 2 ⭐⭐ |
| Г3 | 50′ | 18 | 2 | 13 | 1 | 2 ⭐⭐ |

Формула «от нижней границы `qRange`» удалена: раскладка задана явно у всех
восьми, и диапазонов заданий больше нет — у ступени одно число.

- **Г1** = S4 по времени и числу. Стретч ⭐⭐ обязателен: `stretchRequired`
  меняет текст этапа 6 на «на этой ступени обязателен: спрашивать не нужно,
  отказа нет». Одно задание основы — уровня CEMC ⭐.
- **Г2** = Г1, в `[КОНТЕКСТ]` вернулась строка «Доля русского ≤30% — только под
  новыми терминами, разборы по-английски».
- **Г3** = Г2 плюс `examFriday`: по пятницам в `[КОНТЕКСТ]` приходит «Сегодня
  пятница — экзаменационный режим: 8 заданий на время, без подсказок и образцов;
  разбор после ИТОГа». В другие дни строки нет.

Переходы между ступенями — только кнопкой, условие прежнее.

### 2.4 Чистка

- `lesson.n` больше не пишется (читателей не было).
- `CONTENT.pack` и `CONTENT.hasLessons` удалены вместе с картой `packs` —
  вызовов не было. `CONTENT.phaseBlocks` **оставлен**: его зовёт `waterfall.js`.
- Мёртвая переменная `cemcFlag` в `prompts.js` удалена.
- `Waterfall.second()` больше не отдаёт вторым уроком блок `track: 'all'`:
  субботний блок занимает день целиком, а «вторая дорожка» обязана быть другой
  дорожкой. Если других уроков нет — второго урока нет.
- При загрузке `cleanInjected` выбрасывает из списков показанных долгов всё, что
  не `open`. Ключи уроков не удаляются: пустой список значит «гасить нечего», а
  отсутствие ключа — «ограничения нет», и подмена одного другим разрешила бы
  «Погашено» больше, чем было показано.

**`d.forceLesson` не тронут — см. «Противоречия».**

---

## Цифры миграции (копия живого экспорта, `study-v2-2026-09-02.json`)

Схема осталась `3`; новых разовых правок пакет не вводит. Все контрольные числа
2.7.0 держатся без изменений:

| | было | стало |
| --- | --- | --- |
| долгов всего | 43 | 43 |
| открытых | 42 | **14** (письмо 7 · математика 7) |
| на «1/2» | 6 | **6** |
| закрыто / чек-лист / слито / удалено | — | 2 / 13 / 13 / 1 |
| категорий с двумя открытыми | — | **0** |
| банк слов | 99 | 96 |
| дедлайны | — | content 16 · spread 36 · user 0 |
| серия | 46 | **46** |

Новое в 2.7.3 на той же копии:

| | было | стало |
| --- | --- | --- |
| id в списках показанных долгов (`injected`) | 8 | **3** |
| ключей уроков в `injected` | 1 | 1 |
| `state.hw` | — | `{}` |
| `settings.schoolCourses` | — | 4 курса |

Повторный прогон миграции даёт ноль изменений — идемпотентность сохранена.
Живой файл в `~/Downloads` не тронут: миграция шла на копии в скрэтчпаде.

---

## Тесты

**Было 1966 → стало 2124**, зелёные, ноль провалов.

Новые файлы:

| файл | что покрывает |
| --- | --- |
| `cases.questions-272.js` | карточка вопросов, слова добором, перевод «to explain», радар в будни |
| `cases.hw-273.js` | ДЗ-урок целиком, критерии письма, категории «Бизнес», стретч Г1–Г3, экзаменационная пятница |
| `cases.cleanup-273.js` | удалённые функции, `second()` и общий блок, чистка `injected` |

Имена новых групп в `cases.hw-273.js`: «ДЗ-урок: курсы школы и их дорожки» ·
«ДЗ-урок: кнопка, счётчик и лимит трёх» · «ДЗ-урок: выходные и закрытая норма
дня» · «ДЗ-урок: кнопка на „Сегодня“» · «ДЗ-урок: промпт собран из блока [ДЗ]» ·
«ДЗ-урок: доска долгов — своей дорожки» · «ДЗ-урок: разбор ИТОГа» · «ДЗ-урок:
программный урок остаётся непройденным» · «ДЗ-урок: критерии письма в конце
промпта» · «ДЗ-урок: стретч ⭐⭐ всегда по желанию» · «долги: категории дорожки
„Бизнес“» · «ступени: стретч обязателен на Г1–Г3» · «ступени: экзаменационная
пятница на Г3».
В `cases.cleanup-273.js`: «чистка: удалённые функции и поля» · «второй урок дня:
общий блок вторым не берётся» · «чистка injected: остаются только открытые
долги».

По итогам ревью добавлены «ДЗ-урок не выдаёт себя за программный урок дня» и
«долги дорожки без своих категорий (ДЗ по информатике)», плюс проверки смены курса,
итога со счётом 0 и кнопки «Второй урок» на общий блок. Каждый новый кейс
проверен мутацией: возвращаю дефект — кейс краснеет (15 проверок на пяти
мутациях), возвращаю починку — зеленеет.

Переписаны под новые числа ступеней: `cases.prompts.js` (группы «шкала 7.2» и
«разгон Ф0»), `cases.prompt-body.js` (B-06, B-07 — переименована в «промпт B-07:
на Г1–Г3 доля русского названа прямо»), `cases.migrate-v3.js` (таксономия
19 → 24).

Прогон на **24 датах 2026–2028**: 2124 проверки, ноль провалов в каждый день.

---

## Деплой-чеклист для владельца

Чеклист не зависит от номера версии: он читает её из `state.js` и требует, чтобы
та же версия стояла в `sw.js`. Забытая правка `sw.js` теперь видна как отдельная
строка, а не как «чеклист устарел».

1. **Экспорт ДО обновления.** Настройки → «Скачать JSON».
2. Обновить **оба устройства**, на каждом — жёсткая перезагрузка. «Загрузилась
   смесь версий» → перезагрузить ещё раз: это защита, а не поломка.
3. Проверить глазами:
   - версия приложения в Настройках совпадает с номером выпуска;
   - в банке долгов **открыто 14 · на 1/2 — 6 · закрыто 2 · чек-лист 13**;
   - в банке долгов есть группа **«Бизнес» с пятью категориями Б1–Б5**;
   - на «Сегодня» в будний день видна кнопка **«Урок по ДЗ»** и счётчик
     «ДЗ-уроков на неделе: 0/3»;
   - на экране «Программа» виден блок **«К»** с уроками **К.1 … К.10**;
   - промпт урока **Б7.1** содержит **[ГЛОССАРИЙ]**, **[КЛЮЧИ]** стоят
     последними, слова «Погашено» в промпте нет;
   - в субботу приложение выбирает урок блока **К**;
   - кнопка **«Стало легко → S1»** показана только при трёх закрытых уроках со
     средним счётом ≥ 8 за семь дней.
4. **Экспорт ПОСЛЕ обновления** — второй копией.

Автоматический прогон на мигрированной копии живого экспорта: **34 проверки,
все сошлись**.

---

## Противоречия

**1. `d.forceLesson` — писатель есть, пункт 2.4 не выполнен.** *(решено: вариант А)*
ТЗ 2.4 просит удалить ветку `d.forceLesson` «без писателя». Писатель есть и
работает: `lesson.js:321` пишет `d.forceLesson = true` по клику на ссылку
«всё равно хочу урок», которую рисуют `app.js:719` и `lesson.js:201`; ветку
читают `app.js:537`, `app.js:716` и `waterfall.js:235` — это воскресный обход
запрета на урок. Кейс `tests/cases.today.js:194,197` его проверяет.

Удаление сняло бы с воскресенья единственный способ взять урок — а это **выбор
урока**, и правило пачки требует остановки.

- **Вариант А** ← выбран владельцем: оставить как есть, пункт из 2.4 вычеркнут.
  Ручной обход в воскресенье доктрину не нарушает.
- **Вариант Б:** удалить ссылку, ветку и кейс — воскресенье становится
  безусловным выходным.

**2. `CONTENT.phaseBlocks` вызовы имеет — оставлен.**
Пункт 2.4 перечисляет его среди «без вызовов», но `waterfall.js:194` его зовёт.
Удалены только `pack` и `hasLessons`. *Решено самостоятельно: ближе к букве ТЗ
(«без вызовов — удалить»), обратимо, живых данных не трогает.*

**3. Строка «Доля русского» на Г1 переписана под формулировку Г2.**
ТЗ 2.3 задаёт дословный текст только для Г2. На Г1 стояла своя формулировка
(«только для новых терминов и грамматики; остальное — английский»). Обе строки
об одном, и держать два текста для одного правила незачем: Г1 печатает
«Доля русского ≤10% — только под новыми терминами, разборы по-английски».
Отличать «строгие» ступени от S2 (у той тоже ≤30 %) стал явный флаг `ruStrict`,
а не совпадение процентов. *Решено самостоятельно, обратимо.*

**4. Ступени с «RU 0» больше нет.**
Г2 и Г3 стояли на `ru: '0'` («русский выключен»); по ТЗ 2.3 у обеих теперь
≤30 %. Ветка «русский выключен» в `prompts.js` стала недостижимой и удалена
вместе с кейсом B-07 в старой редакции. *Следствие буквы ТЗ, отмечено ради
протокола.*

**5. Экзаменационный режим Г3 вынесен из «Особого» в отдельную строку.**
В `special` он печатался бы все пять дней недели, а режим включается днём
недели. Строка приходит только в пятницу. *Решено самостоятельно, обратимо.*

**6. Лимит ДЗ-уроков считает взятые дни, а не закрытые.**
ТЗ говорит «счётчик ДЗ-уроков на неделе: n/3» без уточнения. Считаем дни, где
`day.hw` проставлен **или** есть запись в `state.hw`: иначе лимит обходится
тремя незакрытыми уроками подряд. *Решено самостоятельно, обратимо.*

**8. Конкурсный урок: контракт печатает «Задание N/8» при трёх задачах.**
`prompts.js` для конкурсного урока подменяет `qRange` на `'3'`, но правило 2
контракта берёт число из `p.slots` (`contractV3`, prompts.js:203–206), а `slots`
подмена не трогает. Преподаватель нумерует «Задание 1/8 … 3/8» и ждёт ещё пяти,
хотя `[ЗАДАЧИ]` и `CONTEST_LINE` дают ровно три. Дефект **не от 2.7.3**: подмена
`qRange` мертва с 4b, когда контракт перешёл на раскладку. Живой: субботние
уроки блока К — конкурсные. Починка — одна строка (подменять `slots` вместе с
`qRange`), но это правка промпта программного урока вне ТЗ пакета. **Жду слова.**

**9. Длина спринта в промпте называется дважды и по-разному.**
`stageLine` печатает `p.sprintLabel || '~' + lessonMin/2 + ' минут'`, а
`stagesBlock` — `p.sprintLabel || '~13–15 минут'`. `sprintLabel` задан только у
S0, поэтому сегодня строки совпадают, но на S1 промпт скажет «спринты ~18 минут»
в строке ступени и «Спринт 1 (~13–15 минут)» тремя строками ниже. Дефект **не от
2.7.3**, но включается первым же нажатием «Стало легко → S1» — то есть кнопкой,
которую приложение само предлагает. Какое число верное, решает ТЗ, не я.
**Жду слова.**

**7. После выбора курса промпт кладётся в буфер сразу.**
ТЗ описывает состояние и промпт, но не то, как ученик его получает. Без этого
после выбора курса оставался тост и никакого урока. Взятый ДЗ-урок теперь
показывает на «Сегодня» карточку с кнопками «Скопировать промпт ДЗ» и «Вставить
итог урока», а после ИТОГа — счёт. *Решено самостоятельно, обратимо.*

---

## Ревью перед деплоем

Перед пушем прогнан состязательный разбор пакета: шесть независимых линз
(корректность, регрессии, текст промпта, сохранность данных, интерфейс, качество
тестов), каждая находка проверялась тремя скептиками с установкой «опровергнуть».
**42 заявки → 12 подтверждено.** Шесть дефектов были внесены самой 2.7.3 и
починены до пуша; каждая починка закреплена кейсом, и каждый кейс проверен
мутацией (ломаю код — кейс краснеет).

| что | где | почему это было плохо |
| --- | --- | --- |
| **блокер:** закрытый ДЗ-урок занимал пункт «Урок» на «Сегодня» | `lesson.js`, `app.js` | id ДЗ-урока лежит в `day.lessons` (так велит ТЗ), а `Lesson.current`/`dayLesson`/`lessonItem` брали его за урок дня: в заголовке сырой `HW-2026-09-15-math`, в подписи `undefined`, в теле ложное «Уроки текущих фаз закрыты», а кнопки «Скопировать промпт» на экране не оставалось вовсе |
| кнопка «Второй урок» обходила новый фильтр | `lesson.js:260` | свой запасной `State.nextLesson()` возвращал ровно тот урок блока `track:'all'`, который `Waterfall.second()` только что отверг |
| доска долгов дорожки `cs` всегда «чисто» | `state.js` | долг с урока информатики ложится под префикс кода (`П` → письмо, `М` → математика), а доска спрашивалась по `'cs'` — контракт требовал двух долгов ПРИОРИТЕТ, которых на экране нет. До 2.7.3 было недостижимо: уроков `cs` не существовало, ДЗ по ICS3U их создало |
| ошибочно выбранный курс ДЗ не отменить | `app.js` | промах по соседней строке списка запирал день на чужой дорожке и сжигал слот недели. Теперь до ИТОГа есть «Не тот курс? Сменить»; после ИТОГа курс не меняется |
| промпт ДЗ метил долги показанными при неудачном копировании | `lesson.js` | ветка ДЗ не дожидалась результата `UI.copy`; ротация внимания сдвигалась за долги, которых никто не видел |
| два кейса проходили на сломанном коде | `tests/cases.hw-273.js` | проверка глоссария попадала в текст контракта, а не в блок; ветка «ИТОГ со счётом 0» не покрыта |

**Одно последствие починки блокера стоит назвать отдельно:** на дне с ДЗ-уроком
пункт «Урок» теперь показывает программный урок дня как незакрытый — с кнопкой
промпта. Норма дня при этом закрыта (очки, серия, лимит ДЗ считают ДЗ-урок), а
сам ДЗ-урок живёт в своей карточке ниже. Это прямое чтение ТЗ («программный урок
остаётся на следующий день»), но экран стал разговорчивее: если нужно, чтобы
после ДЗ пункт «Урок» гас, это одна строка.

Два подтверждённых дефекта **не чинились** — они существовали до пакета и лежат
вне его ТЗ, см. «Противоречия», пункты 8 и 9.

---

## Решения владельца при приёмке

1. **`d.forceLesson`** — вариант А: оставить, пункт 2.4 вычеркнут. Ссылка «всё
   равно хочу урок» в воскресенье — законный ручной обход, доктрину не нарушает.
2. **Код онлайн-курса** — экран настройки не нужен. Код заменяется точечной
   правкой дефолта и значения в `settings` в 2.7.4, по ответу консультанта.
3. **`GLC2O` в двух списках** — оставляем: механики разные (школьное ДЗ против
   назначения программного урока водопадом).
4. **Стретч на ДЗ-уроке** — всегда «по желанию», на любой ступени. Реализовано
   в `hwParams`, см. 2.1.

---

## Отложено

Раздел «Отложено» отчёта 2.7.0 закрыт целиком: радар в будни — 1.4, категории
`biz` — 2.2, раскладка ступеней S2–Г3 — 2.3.

Новое отложенное:

- **Код онлайн-курса информатики.** `ICS3U` — предположение; заменяется в 2.7.4
  по ответу консультанта, правкой дефолта `SCHOOL_COURSES` и значения в
  `settings.schoolCourses`.
- **Экзаменационная пятница на ДЗ-уроке.** На Г3 в пятницу в `[КОНТЕКСТ]`
  приходит «8 заданий на время, без подсказок и образцов» — и на ДЗ-уроке тоже,
  где заданий столько, сколько принёс ученик, а блок `[ДЗ]` велит вести по
  одному с разбором. Решение владельца по стретчу на ДЗ-уроке сюда не
  распространялось, поэтому режим оставлен по букве ТЗ. До Г3 ещё пять нажатий
  кнопки ступени — **пересмотреть, когда дойдёт**.

---

## Что придёт следом

- **2.7.4** (≈14.09, после пакета №2 от учителей): опорные задания Б9, Б10 и
  К.4–К.6; возможный сдвиг дедлайнов Б10/Б14 под дату OSSLT — через
  `deadlineSource: 'content'`, без отдельной миграции.
- **2.7.5** (≈05.10): опорные задания Б11–Б16, К.7–К.10.

Нумерация контентных пакетов сдвинулась на два: номера 2.7.2 и 2.7.3 забрали
этот пакет механики.

---

## Документы

- ТЗ пакета «Корень»: `TZ-v2.7.0-koren.md`, отчёт по нему: `REPORT-2.7.0.md`.
- Снапшоты промптов сняты с **мигрированной копии живого экспорта** на 14.09.2026:
  урок Б7.1 (15 564 символа) и ДЗ-урок MHF4U (14 148 символов).
